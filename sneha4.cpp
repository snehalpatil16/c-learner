#include<iostream>
using namespace std;
recursion(int);
{
int n,series;
cout<<"enter the number of elements in fibbonacci series";
cin>>n;
recursion(int){
series=recursion(n-1);
cout<<"required fibbonacci series is"<<series;
}return 0;
}
